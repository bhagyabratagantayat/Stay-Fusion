<% layout('layouts/boilerplate') -%>

<body>
    <h3>Listing Details</h3>
    <ul>
        <li><%= listing.title %></li>
        <li><%= listing.description %></li>
        <li>Price: &#8377;<%= listing.price.toLocaleString("en-IN") %> per night</li>
        <li>Location: <%= listing.location %>, <%= listing.country %></li>
    </ul>

    <a href="/listings/<%= listing._id %>/edit">Edit Listing</a>

     <br>
     <br>
    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
        <input type="hidden" name="_method" value="DELETE">
        <button type="submit">Delete Listing</button>
    </form>

</body>





<!-- <p><%= listing.title %></p>
    <p><%= listing.description %></p>
    <p>Price: $<%= listing.price %> per night</p>
    <p>Location: <%= listing.location %>, <%= listing.country %></p>


    <% if (listing.images && listing.images.length > 0) { %>
        <img src="<%= listing.images[0] %>" alt="Listing Image" style="width:200px;height:auto;">
    <% } else { %>
        <p>No image available</p>
    <% } %> -->